# Chummer.Hub.Registry.Contracts

Immutable contract package for Hub registry artifacts, publication and moderation workflow, install projections, compatibility projections, and runtime-bundle head projections.

Consumers such as `chummer.run-services` and presentation depend on this package boundary for shared registry DTOs and keep orchestration, persistence, and UX-specific shaping in their own repos.
